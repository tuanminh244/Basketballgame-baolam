'use client';

export default function DashboardRoute() {
  return (
    <div className="p-6 flex flex-col justify-center items-center min-h-[50vh]">
      <h1 className="text-2xl font-bold text-gray-900 mb-2">Tổng Quan (Mẹ)</h1>
      <p className="text-gray-500 text-sm">Chờ kết nối giao diện CheckerDashboardPage...</p>
      
      {/* TODO: Replace scaffold with real CheckerDashboardPage component
        import CheckerDashboardPage from '@/components/checker/dashboard/CheckerDashboardPage';
        
        <CheckerDashboardPage />
      */}
    </div>
  );
}
